[alias]
	ctxdiff = difftool -y -x \"diff --color -c\"

[includeIf "gitdir:~/.homesick/repos/"]
                   path = ~/.gitconfig-gitbmc

[includeIf "gitdir:~/.gitbmc/"]
                   path = ~/.gitconfig-gitbmc

[include]
                   path = ~/.gitconfig-local
